services:
  app:
    image: ghcr.io/ifuryst/llm-wiki:beta
    container_name: llm-wiki
    restart: unless-stopped
    network_mode: host
    env_file:
      - ${LLM_WIKI_ENV_FILE:-/opt/llm-wiki/.env.docker}
