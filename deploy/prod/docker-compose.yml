services:
  app:
    image: ghcr.io/ifuryst/docmesh:beta
    container_name: docmesh
    restart: unless-stopped
    network_mode: host
    env_file:
      - ${DOCMESH_ENV_FILE:-/opt/docmesh/.env.docker}
