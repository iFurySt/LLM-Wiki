# Test Results

This directory stores durable records of validation runs.

Suggested split:

- `unit/`: unit test summaries
- `e2e/`: integration and end-to-end test summaries
- `perf/`: performance and benchmark summaries

Only store concise summaries and stable artifacts that are useful for future comparison.
