# 2026-04-07 MCP Validation

## Scope

Validated the first LLM-Wiki MCP implementation:

- remote MCP endpoints in the Go service
- Streamable HTTP and legacy SSE compatibility
- LLM-Wiki-oriented MCP tools and resources
- `@ifuryst/llm-wiki-mcp` npm package for stdio usage via `npx`

## Commands Run

```bash
go test ./...
node npm/llm-wiki-mcp/bin/llm-wiki-mcp.js --help
npm install --prefix npm/llm-wiki-mcp --package-lock=false
cd npm/llm-wiki-mcp && node -e "import('@modelcontextprotocol/sdk/server/mcp.js').then(()=>import('zod')).then(()=>console.log('npm sdk ok'))"
```

## Results

- `go test ./...`: passed, including new MCP e2e coverage using the official Go SDK client
- Node stdio package help output: passed
- npm dependency install for `@ifuryst/llm-wiki-mcp`: passed
- Node import smoke test for official MCP SDK and `zod`: passed

## Covered Paths

- `/mcp`
- `/sse`
- `npm/llm-wiki-mcp/`
