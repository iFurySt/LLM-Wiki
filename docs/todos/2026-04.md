# 2026-04 Todos

## Current Priorities

- Make the first hosted end-to-end flow work: admin configures OAuth, user signs in, user and personal tenant auto-create, CLI login persists usable local config.
- Reframe the service contract so CLI, HTTP, MCP, and future protocol clients all sit on top of the same core semantics.
- Define the tenant or workspace membership model so non-admin users can create additional tenants and invite collaborators.
- Design the first downstream adapter contract with Obsidian as the first target.
- Upgrade the official skill to be more opinionated and out-of-box for hosted users and local agents.
- Add structured error model and API versioning conventions.
- Add CI-oriented wrappers for unit, e2e, and perf runs.
- Add hard delete or purge policy only after archive semantics are defined.
- Add better route-level validation errors instead of plain string messages.
- Add markdown rendering for the document viewer in the HTML page.
- Add recent changes and activity surfacing into the wiki UI.
- Add search entry points into the UI and install skill guidance.
- Add direct binary download affordances into the UI beyond the installer script.
- Add auth activity and session inspection to the admin UI.
- Add richer MCP resources and prompts beyond the first document-oriented tool set.
- Add release notes generation and changelog discipline for GitHub tag releases.
- Add install docs for containerized production deployment with PostgreSQL and Redis examples.
- Remove legacy npm token usage from repo and GitHub secrets now that trusted publishing is live.

## Near-Term Followups

- Decide the public terminology split between `tenant`, `workspace`, and `namespace`, including what is user-facing versus internal-only.
- Decide first-login policy knobs: who can self-sign-up, who gets an automatic personal tenant, and how collisions are handled.
- Decide Google and GitHub OAuth provider configuration shape in the admin UI and config model.
- Decide the CLI profile format under `~/.llm-wiki/` before widening auth flows further.
- Decide the first stable protocol boundary before implementing many bespoke clients.
- Decide whether Obsidian should start as export, sync, or a fully interactive plugin workflow.
- Decide whether to stay on direct `pgx` SQL or move later to `sqlc`.
- Decide first-class document content shape beyond markdown blob.
- Decide whether `sources` are in the next migration batch or deferred.
- Decide whether benchmarks should remain Go-native or later move to a dedicated load tool.
- Decide whether to expose install assets with embedded content, object storage, or release attachments long-term.
- Decide whether first-party browser login should be backed by an external IdP integration first or a built-in authorization server.
- Decide whether release assets should also be mirrored to a CDN or package manager beyond GitHub Releases.
