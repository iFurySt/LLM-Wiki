# 2026-04 Worklog

## 2026-04-07

- Bootstrapped repository documentation.
- Added `AGENTS.md` to define repo intent and point to the docs system of record.
- Created initial docs structure for knowledge, plans, todos, decisions, worklog, and references.
- Chose `LLM-Wiki` as the working product name.
- Initialized Go module and repository skeleton.
- Added `cmd/server` and `cmd/cli`, with CLI intentionally kept as a thin HTTP wrapper.
- Added config loading, Zap logger setup, Gin HTTP server, and bootstrap endpoints.
- Added local development Docker Compose for PostgreSQL.
- Verified local bootstrap by running the server and fetching `system info` through the CLI over HTTP.
- Reorganized docs into `knowledge`, `plans`, `todos`, `decisions`, `worklog`, `references`, and `test-results`.
- Added PostgreSQL wiring with startup migrations and readiness checks.
- Implemented v0 folder and document CRUD with revision tracking.
- Extended the CLI with thin HTTP wrapper commands for folders and documents.
- Added unit tests, e2e tests backed by real PostgreSQL containers, and a first benchmark harness.
- Added folder and document list APIs, document slug lookup, and document archive lifecycle.
- Hardened service-side key validation and updated test coverage for the expanded API surface.
- Added folder archive support, document status filtering, and structured HTTP error responses.
- Added a simple Gin-served HTML UI for folders and documents.
- Moved local host-exposed ports away from common defaults and added the Go service to Docker Compose with `make dev` as the main startup path.
- Upgraded the HTML UI into a retro wiki-style operator console with theme switching, filters, document inspection, editing, archiving, and install affordances.
- Reworked the HTML UI from an operator-heavy console into a reader-first wiki layout with left-side `ns` and document navigation, top install tabs, quieter dark styling, and collapsed editing tools.
- Refined the wiki UI again toward the original retro shell, replacing the purple-heavy theme with a black-and-yellow palette, adding IDE-style folder document trees, and rendering document bodies as markdown on the right.
- Split the web UI into separate `/ui` wiki and `/ui/install` install pages, with the install page focused on CLI, skill, and MCP cards that each expose copyable agent prompts, URLs, and human shell commands.
- Simplified the hosted install guide to CLI-only instructions and updated the CLI installer to detect existing installs, skip no-op reinstalls when already current, and upgrade in place when needed.
- Added `LLM_WIKI_INSTALL_BASE_URL` so hosted install docs and UI install prompts render from a configured public base URL instead of hard-coded host strings, and aligned README plus install references around that model.
- Added hosted install surfaces under `/install/*`, including a markdown guide, shell installer, and packaged skill downloads.
- Added an official `llm-wiki` skill in-repo with CLI, HTTP, and installation references.
- Added cross-platform CLI packaging into `dist/install/` and wired packaging into `make dev` and the dev Docker image build.
- Added official MCP support to the Go service with Streamable HTTP at `/mcp` and legacy SSE compatibility at `/sse`.
- Added first-party LLM-Wiki MCP tools and resources on the Go server side, plus e2e coverage using the official Go SDK client.
- Added `npm/llm-wiki-mcp`, a publishable stdio MCP bridge package intended for `npx`-based local integrations.
- Added GitHub Actions release automation so pushed tags publish CLI archives and install assets to GitHub Releases.
- Switched the CLI installer to pull binaries from GitHub Releases instead of local server-hosted archives.
- Expanded tag release automation to also publish the main service image to Docker Hub and GHCR, and publish `@ifuryst/llm-wiki-mcp` to npm.
- Added production-oriented Docker build inputs and durable install docs under `docs/install/`.
- Tightened npm publish validation after the first tag run showed a missing tracked stdio bridge binary and an npm token policy failure.
- Slimmed the production Docker build so the registry image only compiles the main service instead of generating the full CLI release bundle.
- Switched npm package publishing from long-lived token auth to npm trusted publishing over GitHub OIDC.
- Split npm publishing into a dedicated `publish-npm.yml` workflow so npm trusted publisher matching stays minimal and deterministic.
- Bumped the npm publish workflow to Node 24 and npm 11+, which was required to make npm trusted publishing succeed.
- Successfully published `@ifuryst/llm-wiki-mcp@0.1.8` through npm trusted publishing and verified Docker release runtime performance dropped from the original 28-minute path to a low-minute path.
- Switched the local `make dev` app container from rebuild-only image startup to source-mounted hot reload with automatic install asset repackaging on watched file changes.
- Changed the CLI default base URL to `https://llm-wiki.ifuryst.com/` while keeping local development overrideable through `LLM_WIKI_CLI_BASE_URL` and explicit flags.
- Added a `main` branch beta workflow that publishes `ghcr.io/ifuryst/llm-wiki:beta` and can SSH-deploy the image to amoylab.
- Added production Compose deployment assets and durable amoylab cutover notes for moving from a native `systemd` process to a Docker-managed service.
- Replaced the old bootstrap auth header flow with bearer-token auth, service principals, browser/device login flows, CLI profile storage under `~/.llm-wiki/config.json`, and scoped MCP authorization.
- Added first-boot setup at `/setup`, a pixel-style admin console at `/admin/*`, and username/password-backed approval for browser and device-code login flows.
- Added admin-managed OAuth provider configuration, OAuth callback handling, first-login auto user creation, personal-`ns` auto provisioning, and auth e2e coverage for the hosted browser login path.
- Added multi-`ns` creation, invitation acceptance, `ns` switching, and CLI coverage for the new membership flows.
- Added an initial Obsidian adapter at the CLI layer through export commands, exporting folder markdown plus LLM-Wiki metadata manifest for downstream vault usage.
- Defined a service-first live-browse protocol direction for downstream clients and added the first Obsidian desktop plugin that reads LLM-Wiki directly over HTTP instead of exporting vault files.
- Extended the Obsidian plugin with a mirror flow that writes the current `ns` into `LLM-Wiki/<ns>/...` inside the active vault so remote docs also appear in Obsidian Files.
- Removed the Obsidian live-browser view and simplified the plugin into a sync-first adapter that mirrors the current CLI `ns` on load and on a configured interval.
- Reworked the hosted `/ui` experience away from the old pixel-heavy shell into a denser document ns with a tighter left file tree, reduced visual noise, and a simpler install page.
- Replaced the server-rendered left file tree on `/ui` with an embedded `@mui/x-tree-view` React island, keeping the rest of the page server-rendered while making the directory navigation easier to iterate.
- Changed `/ui` navigation from full-page reloads to fragment fetches plus `history.pushState`, so tree selection, install tab switches, and status filter changes now update only the content region instead of rebuilding the whole layout.
- Split the wiki reader into its own fragment route so normal document switching now replaces only the right-side reading pane instead of re-rendering the whole wiki content block.
- Added repo-level collaboration and history guides, tightened `AGENTS.md` into a must-read index, and clarified when contributors must read skill, install, and commit-time documentation before making or shipping changes.
- Added a first-party `chrome-devtools-cli` skill plus UI browser-collaboration rules, requiring Chrome stable and reuse of `.llmwiki/chrome-profile` for browser debugging and acceptance.
- Added repo browser helpers so contributors can launch Chrome stable with `.llmwiki/chrome-profile` and connect `chrome-devtools-mcp` to the shared `9222` instance instead of drifting onto the default browser profile.
